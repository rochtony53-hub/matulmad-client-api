# Port (Render le fournit automatiquement)
PORT=4000
# Base MongoDB (LA MÊME que le backend existant — collection users séparée)
MONGO_URI=mongodb+srv://...
# Secret JWT (générer une longue chaîne aléatoire)
JWT_SECRET=change_me_long_random
# Pont vers le backend "core" (WebView + admin + auto-validation)
CORE_API_BASE=https://sms-gateway-admin-backend-vw8p.onrender.com
CORE_API_TOKEN=eyJ...   # JWT admin/service du backend core
# Base du WebView de paiement
PAYMENT_WEBVIEW_BASE=https://payment-webview.vercel.app
